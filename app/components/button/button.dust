<button class="c-button {class}" {?id}id="{id}"{/id} {?type}type="{type}"{/type}>
    <span>{buttonText}</span>
    {?buttonIcon}{>"svg-icon" name="{buttonIcon}" class="{buttonIconClass}"/} {/buttonIcon}
</button>
